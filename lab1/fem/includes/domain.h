#pragma once

/// NOTE: This file only includes ALL various Domain structures at once.
/// Please include this file and then use namespaces (like `using namespace fem::two_dim`) 
/// to select what Domain realization you need.
/// In general, you can include only one of headers below, but for what?

#include "../two_dim/two_dim_domain.h";
